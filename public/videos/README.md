Place a **4–6 second** muted looping **MP4** (or WebM) at `kiosk-landing.mp4` for the kiosk landing background, or set `NEXT_PUBLIC_LANDING_VIDEO_URL` in `.env` to any HTTPS URL.

The app falls back to a gradient-only hero if the file is missing or the video fails to load.
